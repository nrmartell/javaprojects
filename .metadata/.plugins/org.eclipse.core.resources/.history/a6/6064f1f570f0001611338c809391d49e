public class BoxType {

	
//	MainProg readObject = new MainProg();
//	MainProg.readFile();
	
	double getVolume(){
		
		
		double volume= 0;
		//need to pull data from array in MainProg to get volume of boxes
		for(int i = 0; i< boxData.length; i++){
			double volume = boxData[i][0] * boxData[i][1] * boxData[i][2];
		}
//		
		return  volume;
	}


	boolean fitsInBoxType(Basketball b){
		return true;
	}
	
	double emptySpace(Basketball b){
		double spaceDifference = 0;
		
		return spaceDifference;
		
		
	}


}
